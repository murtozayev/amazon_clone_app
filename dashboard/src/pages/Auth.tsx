import Login from "@/components/shared/Login"

const Auth = () => {
    return (
        <div className="w-full h-screen banner flex items-center justify-center">
            <Login />
        </div>
    )
}

export default Auth